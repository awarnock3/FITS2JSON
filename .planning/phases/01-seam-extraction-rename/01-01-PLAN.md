---
phase: 01-seam-extraction-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/listhead.c
  - src/fits2json.c
  - src/Makefile
  - README.md
autonomous: true
requirements:
  - BLD-02
  - HEAD-03
must_haves:
  truths:
    - User invokes `fits2json` and the existing CFITSIO filename selector forms still enter the same HDU-selection path.
    - The implementation entry point lives in `src/fits2json.c`, not `src/listhead.c`.
    - The build produces only `fits2json`; no `listhead` compatibility binary or alias remains.
    - Usage/help/examples refer to `fits2json` consistently.
  artifacts:
    - path: src/fits2json.c
      provides: Renamed single-file CLI entry point with preserved selector flow
      min_lines: 50
    - path: src/Makefile
      provides: Single-binary build for `fits2json`
      contains: "fits2json"
    - path: README.md
      provides: Top-level program naming aligned to `fits2json`
      contains: "fits2json"
  key_links:
    - from: src/fits2json.c
      to: CFITSIO selector behavior
      via: "existing `fits_open_file` + `fits_get_hdu_num` + selector detection flow"
      pattern: "fits_open_file|fits_get_hdu_num|strchr\\(argv\\[1\\], '\\['\\)"
    - from: src/Makefile
      to: src/fits2json.c
      via: source and target names
      pattern: "fits2json\\.c|^fits2json:"
    - from: src/fits2json.c
      to: README.md
      via: program name in usage/help/examples
      pattern: "fits2json"
---

<objective>
Rename the brownfield CLI seam from `listhead` to `fits2json` without changing the existing CFITSIO extension-selection workflow.

Purpose: Satisfy Phase 1's rename seam so later JSON work can build on the new entry point instead of the legacy filename and binary name.
Output: A renamed C entry point, matching Makefile target, and user-facing help/docs that refer only to `fits2json`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-seam-extraction-rename/01-CONTEXT.md
@src/listhead.c
@src/Makefile
@README.md

<interfaces>
From `src/listhead.c`:
```c
int main(int argc, char *argv[])
{
    fitsfile *fptr;
    char card[FLEN_CARD];
    int status = 0;
    int single = 0, hdupos, nkeys, ii;

    if (!fits_open_file(&fptr, argv[1], READONLY, &status))
    {
      fits_get_hdu_num(fptr, &hdupos);
      if (hdupos != 1 || strchr(argv[1], '[')) single = 1;
```

From `src/Makefile`:
```make
SRCS = $(SRCDIR)/listhead.c
all: listhead
listhead: $(OBJS)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the CLI entry source and preserve selector behavior</name>
  <read_first>src/listhead.c, .planning/phases/01-seam-extraction-rename/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <files>src/listhead.c, src/fits2json.c</files>
  <action>Rename `src/listhead.c` directly to `src/fits2json.c` per D-02 and keep the program as a single C translation unit per D-04. Update only the user-facing usage/help/example strings to say `fits2json` per D-03. Preserve the current CFITSIO extension-selection behavior for whole-file and selected-HDU invocation by keeping the existing `fits_open_file`, `fits_get_hdu_num`, loop, and `single` selector detection logic functionally equivalent; do not add JSON conversion code, wrappers, new modules, or alternate parsing paths.</action>
  <acceptance_criteria>`test -f src/fits2json.c && test ! -f src/listhead.c` passes; `grep -n "fits_open_file\\|fits_get_hdu_num\\|fits_movrel_hdu" src/fits2json.c` finds the existing traversal calls; `grep -n "fits2json file\\.fits" src/fits2json.c` finds renamed examples; `grep -n "listhead" src/fits2json.c` returns no matches.</acceptance_criteria>
  <verify>
    <automated>cd src && make clean && make fits2json && ./fits2json ../testdata/IRPH0189.HDR[0] | grep -F "Header listing for HDU #1:"</automated>
  </verify>
  <done>`src/fits2json.c` is the only runtime entry source, help text names `fits2json`, and selected-HDU invocation still reaches the existing CFITSIO header-listing path.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget the build and top-level naming to `fits2json` only</name>
  <read_first>src/Makefile, README.md, .planning/phases/01-seam-extraction-rename/01-CONTEXT.md, .planning/ROADMAP.md</read_first>
  <files>src/Makefile, README.md</files>
  <action>Update `src/Makefile` so the source list, object derivation, link target, and clean target build only `fits2json` from `src/fits2json.c` per D-01 and D-02. Do not keep a `listhead` alias, secondary target, symlink recipe, or compatibility rule. Update `README.md` and any normal user-facing naming in this phase's touched files to refer to `fits2json` only per D-03. Keep cleanup strictly limited to rename-required changes; do not fold in unrelated `pkg-config`, CI, or test-target repairs in this phase.</action>
  <acceptance_criteria>`grep -n "^SRCS = .*fits2json\\.c$" src/Makefile` passes; `grep -n "^all: fits2json$\\|^fits2json:" src/Makefile` passes; `grep -n "listhead" src/Makefile README.md` returns no matches; `cd src && make clean && make fits2json && test -x fits2json && test ! -e listhead` passes.</acceptance_criteria>
  <verify>
    <automated>cd src && make clean && make fits2json && ./fits2json ../testdata/IRPH0189.HDR | grep -F "Header listing for HDU #1:"</automated>
  </verify>
  <done>The Makefile produces a single `fits2json` executable from `src/fits2json.c`, and repository-facing naming no longer instructs users to run `listhead`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argument -> `fits_open_file` | Untrusted file path and CFITSIO selector syntax crosses from shell input into FITS parsing. |
| `src/Makefile` -> built executable name | Build metadata defines the binary users invoke; mismatched names create ambiguous execution paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/fits2json.c` selector seam | mitigate | Preserve the existing CFITSIO-driven open/HDU-selection flow and avoid introducing shell execution, wrapper parsing, or alternate selector handling during the rename. |
| T-01-02 | R | `src/Makefile`, `README.md`, CLI help text | mitigate | Rename source, build target, and user-facing help/examples together so there is one auditable executable identity: `fits2json` only. |
</threat_model>

<verification>
- `cd src && make clean && make fits2json`
- `cd src && ./fits2json ../testdata/IRPH0189.HDR | grep -F "Header listing for HDU #1:"`
- `cd src && ./fits2json ../testdata/IRPH0189.HDR[0] | grep -F "Header listing for HDU #1:"`
- `grep -Rnw "src/Makefile" -e "listhead" ; test $? -eq 1`
- `grep -Rnw "src/fits2json.c" -e "listhead" ; test $? -eq 1`
</verification>

<success_criteria>
- `src/listhead.c` no longer exists and `src/fits2json.c` contains the runtime entry point.
- `make` inside `src/` produces `fits2json` and does not leave a `listhead` compatibility artifact.
- Help/examples and top-level naming use `fits2json`.
- Existing CFITSIO extension-selection behavior remains reachable through the renamed code path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-seam-extraction-rename/01-01-SUMMARY.md`
</output>
